<main>
  <h1><%= title %></h1>

  <!-- ✅ Fix: Use `<%- grid %>` instead of `<%= grid %>` -->
  <div id="inv-display">
    <%- grid %>
    <!-- ✅ This ensures HTML is rendered instead of displayed as text -->
  </div>
</main>
